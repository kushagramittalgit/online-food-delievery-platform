export interface IUserLogin
{
  email: string;
  password: string;
}
// bydefault all the fields of interfaces are required.
// If we want to make any field optional, we use ? symbol before that field name.
